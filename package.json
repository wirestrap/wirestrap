{
  "name": "wirestrap",
  "private": true,
  "scripts": {
    "build:test:css": "sass tests/sass/wirestrap-test.scss tests/Browser/dist/wirestrap.css --load-path=node_modules --style=compressed --no-source-map",
    "build:test": "npm run build:test:css && vite build --config tests/vite.config.test.js"
  },
  "devDependencies": {
    "@floating-ui/dom": "^1.7",
    "bootstrap-icons": "^1.13.1",
    "playwright": "^1.62.1",
    "sass": "^1.102.0",
    "vite": "^6.0"
  }
}
